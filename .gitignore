/palworld-docker/

# Runtime artifacts
/backups/
docker-compose-*.yml
/.state/
/config/

# Local secrets — never commit. See .env.example for the expected keys.
.env

# Compiled gsa binary — build it on the server with `make`.
/bin/
